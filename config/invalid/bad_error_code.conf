# ERREUR ATTENDUE (sémantique) : 999 n'est pas un code HTTP valide
# (les codes vont de 300 à 599 pour une error_page).

server {
    listen 8080;
    host 127.0.0.1;
    error_page 999 www/errors/404.html;

    location / {
        root www;
        methods GET;
    }
}
