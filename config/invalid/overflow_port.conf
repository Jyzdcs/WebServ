# ERREUR ATTENDUE (syntaxe) : 4294975376 = 2^32 + 8080.
# atoi déborde en silence et redonnerait 8080 -> le parser doit
# rejeter les nombres trop grands AVANT la conversion.

server {
    listen 4294975376;
    host 127.0.0.1;

    location / {
        root www;
        methods GET;
    }
}
