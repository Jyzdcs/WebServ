# ERREUR ATTENDUE (syntaxe) : "8080abc" commence par un chiffre mais
# n'est pas un nombre — atoi l'aurait accepté en silence sans le check.

server {
    listen 8080abc;
    host 127.0.0.1;

    location / {
        root www;
        methods GET;
    }
}
