# ERREUR ATTENDUE (syntaxe) : 'x' n'est pas un suffixe de taille valide
# (seuls k, m, g sont acceptés).

server {
    listen 8080;
    host 127.0.0.1;
    client_max_body_size 10x;

    location / {
        root www;
        methods GET;
    }
}
