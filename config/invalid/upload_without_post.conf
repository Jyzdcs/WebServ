# ERREUR ATTENDUE (sémantique) : upload_store sans POST dans methods —
# c'est POST qui transporte le fichier, l'upload serait impossible.

server {
    listen 8080;
    host 127.0.0.1;

    location /uploads {
        root www/uploads;
        methods GET;
        upload_store www/uploads;
    }
}
