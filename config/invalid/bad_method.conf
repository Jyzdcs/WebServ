# ERREUR ATTENDUE (sémantique) : PATCH n'est pas implémenté par webserv
# (seulement GET, POST, DELETE).

server {
    listen 8080;
    host 127.0.0.1;

    location / {
        root www;
        methods GET PATCH;
    }
}
